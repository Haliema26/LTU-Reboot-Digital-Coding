function  seasonWear(season) {
    switch ( season) {
        case 'spring':
            console.log(`it is ${season}`);
            break;
            case 'summer':
                console.log(`it is ${season}`):
                break;
                case 'autumn':
                    console.log(`it is ${season}`);
                    break;
                    case 'winter' :
                        console.log(`it is ${season}`);
                        break;
                        default :
                        console.log( `it is ${season}`);


    }
}
seasonWear ('winter');